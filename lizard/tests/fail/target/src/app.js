function complex(flags) {
	if (flags.a) {
		if (flags.b) {
			if (flags.c) {
				if (flags.d) {
					if (flags.e) {
						if (flags.f) {
							if (flags.g) {
								if (flags.h) {
									if (flags.i) {
										if (flags.j) {
											if (flags.k) {
												if (flags.l) {
													if (flags.m) {
														if (flags.n) {
															if (flags.o) {
																return 1;
															}
														}
													}
												}
											}
										}
									}
								}
							}
						}
					}
				}
			}
		}
	}
	return 0;
}
